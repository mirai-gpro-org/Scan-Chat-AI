/**
 * Welltect デザイントークン。
 *
 * 【方針】既存コードのクラス名は変えず、**パレットの解決先だけを差し替える**。
 *   brand- と slate- のクラスは 18 ファイル・数百箇所で使われており、名前を変えると
 *   差分が全ファイルに広がってレビュー不能になるため。
 *
 * 【ブランド】顧客向け主ブランド = Welltect / 運営 = Wellfort。
 *   主要顧客 = 日本の法人、主な利用者 = 50〜65代の経営者・役員・幹部。
 *
 *   Warm Ivory     #F7F5EF   ページ地      → slate-50
 *   Executive Navy #102B3A   見出し・ナビ   → navy-800
 *   Precision Teal #287F86   主要アクション → brand-600
 *   Medical Mist   #DCECEF   淡色面・アイコン円 → mist
 *   Graphite       #263238   本文          → slate-900
 *   Quiet Bronze   #A98558   ヘアライン・小面積アクセント専用 → bronze
 *
 * 【Quiet Bronze の制約】ヘアラインと小面積アクセントにのみ使う。
 *   大面積・主要ボタン・カード背景には使わない (レビューで弾く)。
 *
 * 【ステータス色はブランド色と分離する】医療・物流の状態は status.* を使う。
 *   brand (Teal) は「操作」の色であって「状態」の色ではない。
 *   状態表示は必ず アイコン + テキスト + 色 の 3 点セットにし、色だけに依存させない。
 *
 * 【既存の装飾的な青系の中立化】cyan/sky/blue/indigo/violet は装飾で使われていた
 *   (禁止指定の「海外SaaS風」の主因)。navy スケールへ寄せて中立化する。
 *   orange は amber へ寄せる。
 */

/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,ts,tsx,md,mdx,svelte,vue}'],
  theme: {
    extend: {
      colors: {
        // ── 中立色: Warm Ivory → Graphite の暖色寄りグレー ──────────────
        // Tailwind 既定の slate (寒色) を置き換える。text-slate-*/bg-slate-* が
        // そのまま Welltect の中立色になる。
        slate: {
          50:  '#F7F5EF', // Warm Ivory (ページ地)
          100: '#F0EDE3',
          200: '#E3DED1', // 罫線
          300: '#CBC5B6',
          400: '#98A1A3',
          500: '#677275',
          600: '#55636A',
          700: '#3E4C52',
          800: '#313E44',
          900: '#263238', // Graphite (本文)
          950: '#16222A',
        },

        // ── ブランド: Precision Teal (= 操作の色) ────────────────────────
        brand: {
          50:  '#EFF6F6',
          100: '#D7EAEB',
          200: '#B0D5D8',
          300: '#7FB9BE',
          400: '#4E9BA2',
          500: '#33868D',
          600: '#287F86', // Precision Teal
          700: '#1E6167',
          800: '#184C51',
          900: '#133C40',
          950: '#0E2D30',
        },

        // ── Executive Navy (見出し・ナビ・情報表示) ──────────────────────
        navy: {
          50:  '#F1F4F5',
          100: '#DFE7EA',
          200: '#BFCFD6',
          300: '#93AAB5',
          400: '#5F808F',
          500: '#3B6274',
          600: '#274B5C',
          700: '#1B3B4A',
          800: '#102B3A', // Executive Navy
          900: '#0B1E29',
          950: '#071620',
        },

        // ── 単色トークン ────────────────────────────────────────────────
        mist: '#DCECEF',   // Medical Mist
        bronze: '#A98558', // Quiet Bronze (ヘアライン・小面積のみ)

        // ── ステータス (ブランド色と分離。色だけに依存させない) ───────────
        status: {
          ok:      '#2F6B4F', 'ok-bg':      '#E4EEE7', // 基準内 / 完了
          watch:   '#8C6212', 'watch-bg':   '#F3EAD7', // 要注意
          action:  '#A6423A', 'action-bg':  '#F3E3E1', // 要対応
          active:  '#274B5C', 'active-bg':  '#E1E8EB', // 進行中
          unknown: '#5A676C', 'unknown-bg': '#EDEBE4', // 未取得 / 不明
        },

        // ── 既存クラス名の解決先を中立化 ─────────────────────────────────
        // 状態表現に使われていた 3 系統は status と同じ色に寄せる。
        emerald: {
          50:'#EDF4EF',100:'#D8E9DE',200:'#B4D4C0',300:'#86B899',400:'#5A9B73',
          500:'#3F855A',600:'#2F6B4F',700:'#26563F',800:'#1E4432',900:'#183628',950:'#122A1F',
        },
        amber: {
          50:'#FAF5EA',100:'#F3E9D2',200:'#E6D3A6',300:'#D4B771',400:'#C09B46',
          500:'#A8832A',600:'#8F6413',700:'#74500F',800:'#5C400C',900:'#4A3309',950:'#332306',
        },
        red: {
          50:'#FAF0EF',100:'#F4DDDA',200:'#E8BCB6',300:'#D8948B',400:'#C36F63',
          500:'#B15347',600:'#A6423A',700:'#85342E',800:'#692925',900:'#55211E',950:'#3A1614',
        },
        rose: {
          50:'#FAF0EF',100:'#F4DDDA',200:'#E8BCB6',300:'#D8948B',400:'#C36F63',
          500:'#B15347',600:'#A6423A',700:'#85342E',800:'#692925',900:'#55211E',950:'#3A1614',
        },
        orange: {
          50:'#FAF5EA',100:'#F3E9D2',200:'#E6D3A6',300:'#D4B771',400:'#C09B46',
          500:'#A8832A',600:'#8F6413',700:'#74500F',800:'#5C400C',900:'#4A3309',950:'#332306',
        },
        // 装飾で使われていた青系 → navy へ寄せて中立化
        cyan: {
          50:'#F1F4F5',100:'#DFE7EA',200:'#BFCFD6',300:'#93AAB5',400:'#5F808F',
          500:'#3B6274',600:'#274B5C',700:'#1B3B4A',800:'#102B3A',900:'#0B1E29',950:'#071620',
        },
        sky: {
          50:'#F1F4F5',100:'#DFE7EA',200:'#BFCFD6',300:'#93AAB5',400:'#5F808F',
          500:'#3B6274',600:'#274B5C',700:'#1B3B4A',800:'#102B3A',900:'#0B1E29',950:'#071620',
        },
        blue: {
          50:'#F1F4F5',100:'#DFE7EA',200:'#BFCFD6',300:'#93AAB5',400:'#5F808F',
          500:'#3B6274',600:'#274B5C',700:'#1B3B4A',800:'#102B3A',900:'#0B1E29',950:'#071620',
        },
        indigo: {
          50:'#F1F4F5',100:'#DFE7EA',200:'#BFCFD6',300:'#93AAB5',400:'#5F808F',
          500:'#3B6274',600:'#274B5C',700:'#1B3B4A',800:'#102B3A',900:'#0B1E29',950:'#071620',
        },
        violet: {
          50:'#F1F4F5',100:'#DFE7EA',200:'#BFCFD6',300:'#93AAB5',400:'#5F808F',
          500:'#3B6274',600:'#274B5C',700:'#1B3B4A',800:'#102B3A',900:'#0B1E29',950:'#071620',
        },
      },
      fontFamily: {
        /*
         * 欧文・数字は OS の UI フォント、**和文は UD フォント**を当てる。
         * フォントのフォールバックは 1 文字ずつ効くので、この 1 本で
         *   欧文/数字 = system-ui (検査値の tabular-nums がそのまま効く)
         *   和文      = BIZ UDPGothic → Hiragino Sans → Noto Sans JP
         * になる。
         *
         * BIZ UDGothic (モリサワ) を和文の先頭に置く理由:
         *   - **Windows 10 October 2018 Update 以降に OS 標準搭載**なので、
         *     主な利用者 (50〜65 代・Windows) には **追加ダウンロードなし**で届く。
         *     出典: モリサワ ニュースリリース
         *     morisawa.co.jp/about/news/4010
         *   - UD フォントで、装飾を省いて誤読を避け、字画間を広く取る設計。
         *     小さいサイズでも全文字を判別できることを開発基準にしている。
         *   - 和文と英数字・記号が混在したときのばらつきが少ない。
         *   - **等幅版 (BIZ UDGothic) を先に置く**。プロポーショナル版 (UDP) は
         *     句読点を詰めるため、今回指摘のあった「句読点が詰まって読めない」に
         *     逆行する。等幅版は句読点が全角幅のままで、和文本来のベタ組みになる。
         *     欧文・数字は system-ui 側が担当するので等幅の影響を受けない。
         * 未搭載の環境 (macOS/iOS) は Hiragino Sans に落ちる = 従来どおり。
         * 和名も併記するのは、環境によって登録名が和名のことがあるため (保険)。
         * Web フォント (Google Fonts) は入れていない — 和文は実測で 400+700 の
         * サブセットが約 700KB になるため、まず OS 搭載フォントで取りに行く。
         */
        sans: [
          'system-ui',
          '-apple-system',
          'BlinkMacSystemFont',
          '"Segoe UI"',
          'Roboto',
          '"BIZ UDGothic"',
          '"BIZ UDゴシック"',
          '"BIZ UDPGothic"',
          '"Hiragino Sans"',
          '"Noto Sans JP"',
          /*
           * **最後の保険 = 和文の受け皿を必ず 1 つ残す** (2026-09-03)。
           * ここまで 1 つも見つからない環境では `sans-serif` に落ちるが、
           * その解決先は OS の設定次第で、**中国語フォントに当たると漢字が
           * 中国字形で出る** (実測: Linux の既定 sans-serif が WenQuanYi Zen Hei)。
           * Windows/Android に必ず在る和文フォントを手前に挟んでおく。
           * **既存の環境では手前の BIZ UDGothic 等が先に当たるので挙動は変わらない。**
           */
          '"Yu Gothic UI"',
          '"Yu Gothic"',
          '"Meiryo"',
          '"Noto Sans CJK JP"',
          'sans-serif',
        ],
        /*
         * **等幅にも和文の受け皿を置く** (2026-09-03・発注者指摘「漢字が中国語漢字のように見える」)。
         *
         * `font-mono` は数値や出典行に使っているが、**出典行や走りフッターには和文が入る**
         * (「アブストラクト」「医療受診の目安 §1〜§4」「AI疾病予防報告書｜〇〇様」)。
         * Tailwind 既定の mono には和文フォントが 1 つも無いため、和文だけが
         * **ブラウザの既定フォールバック**に落ちていた。実測:
         *   - 発注者の PDF (Windows/Chrome) … 本文=BIZ-UDGothic / **等幅の和文だけ YuGothicUI**
         *   - この環境 (Linux) … 等幅の和文が **WenQuanYi Zen Hei (中国語フォント)**
         * 欧文・数字は従来どおり等幅フェイスが担当し (Consolas 等は漢字を持たない)、
         * **和文だけが本文と同じ BIZ UDGothic に落ちる**。tabular-nums は影響を受けない。
         */
        mono: [
          'ui-monospace',
          'SFMono-Regular',
          'Menlo',
          'Monaco',
          'Consolas',
          '"Liberation Mono"',
          '"Courier New"',
          '"BIZ UDGothic"',
          '"BIZ UDゴシック"',
          '"Hiragino Sans"',
          '"Noto Sans JP"',
          '"Yu Gothic UI"',
          '"Noto Sans CJK JP"',
          'monospace',
        ],
      },
      fontSize: {
        // 本文の下限は 16px。補助情報を小さくしない方針のため、
        // 12px 以下のユーティリティは定義しない (text-xs = 13px に引き上げ)。
        //
        // 【行間 (2026-08 見直し)】16px は iOS/Web の標準的な本文サイズであって
        //   大きくはない。「大きく見える・1 画面に入る量が少ない」の実体は行間の
        //   広さだったので、**文字は縮めず行間を詰める**方針に変えた。
        //   base 1.85rem (1.85) → 1.75rem (1.75)。実測でスマホ 1 画面あたりの
        //   文字数が 330 → 377 字に増える (iPhone 15 Pro 相当)。
        //   1.75 は和文本文の一般的な範囲内で、詰めすぎではない。
        xs: ['0.8125rem', { lineHeight: '1.4rem' }],   // 13px
        sm: ['0.875rem',  { lineHeight: '1.5rem' }],   // 14px
        base: ['1rem',    { lineHeight: '1.75rem' }],  // 16px
        lg: ['1.125rem',  { lineHeight: '1.75rem' }],
      },
      minHeight: { touch: '44px' }, // タップ領域の下限
      minWidth:  { touch: '44px' },
    },
  },
  plugins: [],
};
