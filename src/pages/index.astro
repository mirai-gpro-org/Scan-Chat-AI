---
/**
 * トップページ。
 *
 * 旧設計ではメニュー画面 (ダッシュボード / AI 問診 / AI スキャン) を出していたが、
 * 新設計ではダッシュボード自体をトップに出す方針。
 * ?u= 等のクエリは保持してリダイレクト。
 */
export const prerender = false;

const url = new URL(Astro.request.url);
const target = `/dashboard${url.search}`;
return Astro.redirect(target, 302);
---
