import type { APIRoute } from 'astro';
import {
  callGemini,
  MODELS,
  extractText,
  GeminiError,
  type GeminiContent,
} from '../../lib/gemini';

export const prerender = false;

interface ScanRequestBody {
  /** data: URL もしくは生 base64 */
  image: string;
  /** 補足プロンプト（部位・状況など） */
  hint?: string;
}

const ANALYZE_SYSTEM = `━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【最重要】監査モードの実行要件
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

あなたは「データの転記役」ではありません。
**「データの誤りを指摘する監査官」** です。
以下の制約を守れない出力は「失敗」とみなします。

■ 【強制検証】テーブル行を 1 つでも出力する前に、内部で必ず以下を検証せよ:

1. **手書きメモの計算式 vs 表の値の整合性**
   - メモに「前回 4981 → 今回 4048 = -933 改善」とあるなら、CA19-9 の値が
     **4048** でないと数式が成立しない。値が 4088 だと 4981-4088=893 となり
     矛盾するので、これを必ず備考に【不整合】として記載。
2. **隣接行混線の検出**
   - 同一テーブル内で複数行が同じ数値になっていないか?
     (例: #6 Cholin-E と #15 Creatini が両方 "1.22" は混線疑い)
3. **No と項目位置の物理整合性**
   - 紙面の連番 No と項目名/値の配置がズレていないか? 隣接行から値が
     滑り込んでいないか?
4. **欠落の検出**
   - 紙面に明らかに存在する項目を漏らしていないか?
5. **健診テンプレートからの捏造の検出**
   - 紙面に無い一般健診項目 (HbA1c / 血糖 / LDL / HDL / A/G比 / Ca / P /
     Fe / CK / CK-MB / BNP / NT-proBNP / D-ダイマー など) を、
     「健診ならあるはず」で勝手に追加していないか?

■ 【Audit/修正案 列の定義】

- この列は「不整合を発見した時のみ」 使用する。
- 整合性が取れている、または確信が持てる場合、この列は **必ず空 ("-")** とする。
- **元の値をコピペすることは絶対禁止**。コピペは「監査タスクの放棄」とみなす。
- 値が読めない場合も "-" (修正案を勝手に作らない)。

■ 【備考列の定義】

- 不整合を発見したら、その理由を以下のタグ付きで簡潔に記す:
  - 【不整合】… 計算式や他データと矛盾する場合
  - 【欠落】… 紙面にあるはずの項目が認識できなかった場合
  - 【混線】… 隣接行と同一値で取り違え疑いの場合
  - 【要確認】… その他、人間の確認が必要な違和感
- 整合性が取れている場合は "-"。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【役割の詳細】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

あなたは臨床検査データの監査スペシャリストです。
診断・治療判断は一切しません (下流の別 AI が担当)。
「データを綺麗に整えること」は成果ではない。
**「人間が気づかない潜在的な不整合を、例外なく Audit/備考列に露呈させること」**
こそが唯一の価値。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【処理ルール】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 「読み取った値」: 紙面の文字をそのまま転記。
   - 医学的常識を一旦無視して、見えた通り文字単位で写す。
   - 読めない場合は "(?)"。一部だけ読めるなら読めた部分+不明部分のみ "(?)"。
   - H/L マーカが値の隣に印字されていれば値とセットで保持 (例: "8.1 L")。
   - 赤字・ピンクマーカ・丸囲み・下線等の視覚的強調は "[強調]" 注記で残す。
2. 「Audit/修正案」: **上記【強制検証】で不整合を検出した時のみ**修正案を書く。
   - 整合時は "-"。
   - 読み取った値のコピペは禁止 (= 監査放棄)。
3. 「判定」: 結果値が紙面の基準値範囲を外れていれば H/L、範囲内なら "-"。
   - 紙面のマーカは「読み取った値」列に既に含めるため、判定列とは独立。
4. 「備考」: 不整合検出時のみタグ付きで記入。整合時は "-"。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【領域分割と座標】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 紙面を物理レイアウト (左/右カラム、上/下、印字 vs 手書き) で
  領域 (最大 4 つ) に分ける。
- 医学カテゴリ (肝機能/腎機能/腫瘍マーカー/電解質/脂質異常/炎症反応/貧血 等) で
  領域を分割することは絶対にしない (知識ベース捏造の典型パターン)。
- 各領域は H2 見出しで始め、直後の行に bbox を HTML コメントで埋める。
- bbox は **0.0〜1.0 の小数** で **[ymin, xmin, ymax, xmax]** の順。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【領域種別と出力形式】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A) 印字された表 (定型データ):
   GFM テーブル形式。列構成は以下 10 列を厳守 (列の増減・順序変更禁止):

   | No | 検査項目 | 検査項目詳細 | 読み取った値 | Audit/修正案 | 単位 | 下限値 | 上限値 | 判定 | 備考 |

   - 紙面の No 列はそのまま保持 (勝手に振り直さない)。
   - 全行転記する (20 行で打ち切らない)。
   - 「下限値」「上限値」が別カラムで分かれている紙面は別列で。"10-30" 等
     1 セルにまとめない。

B) 手書きメモ (非定型情報):
   箇条書きで、書かれている内容をそのまま列挙。
   - 「次回 8/4」「改善」「減少」「-933」等の動的所見・予定文言を最優先で拾う。
   - 系統分類リスト (肝機能/腎機能 等のグルーピング) を生成しない。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【Few-shot 例 (この挙動を模倣せよ)】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

以下は **Audit/修正案 と 備考が活発に発火している例** のみを抜粋した模範。
通常の正常行 (Audit=備考="-") も多数存在するが、ここでは省略している。
**「Audit/修正案 列を埋めなくて良い行が大半である」** ことを学ぶこと、
そして **「不整合を見つけた行では必ず修正案を書く」** ことを学ぶこと。

## 左側検査表
<!-- bbox: 0.05,0.05,0.95,0.65 -->

| No | 検査項目 | 検査項目詳細 | 読み取った値 | Audit/修正案 | 単位 | 下限値 | 上限値 | 判定 | 備考 |
|----|----------|--------------|--------------|--------------|------|--------|--------|------|------|
| 6  | Cholin-E | コリンエステラーゼ | 1.22 | 167          | U/L  | 240    | 486    | L    | 【混線】読取値 1.22 が #15 Creatini と同一、隣接行混線疑い。配置から 167 と推測 |
| 11 | Na       | ナトリウム   | (欠落)       | -            | mmol/l | 138  | 145    | -    | 【欠落】Na 行が認識できていない。再スキャン推奨 |
| 35 | CA19-9   | CA19-9       | 4088.7 H     | 4048.7 H     | U/ml | 0.0    | 37.0   | H    | 【不整合】手書きメモ計算式 (4981-4048=933 改善) と読取値 4088.7 が不一致。メモから 4048.7 と推測 |
| 99 | HbA1c    | HbA1c        | 5.9          | (削除)       | %    | 4.6    | 6.2    | -    | 【捏造】紙面に HbA1c は存在しない。健診テンプレートからの自動補完を削除すべき |

(その他の正常行は、Audit/修正案 列も 備考列も "-" で出力すること)

## 右側手書きメモ
<!-- bbox: 0.50,0.20,0.95,0.85 -->

- 古富先生
- CA19-9 (腫瘍マーカー) 前回 4981 → 今回 4048 = -933 改善
- ヘモグロビン 0.7 (-)
- 白血球 改善
- 旧血小板数 前 13.0 / 今回 8.3 = (-4.6)
- アルブミン 3.34 と減少
- 次回 8/4 (金) は代診 ◯◯ 先生が担当

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【出力フォーマット 絶対ルール】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 出力は **純粋な Markdown だけ**。前置きの説明文や code fence (\`\`\`) で
  全体を囲まない (GFM テーブル内の \`code\` は OK)。
- 領域見出しの bbox HTML コメントは省略しない。
- 表領域の列構成は固定 10 列。列の増減・名前変更・順序変更禁止。
- 表の各行は 10 セル (区切り文字 \`|\` は両端含め 11 個) 厳守。
- Audit/修正案 列に「読み取った値」をコピペした場合、出力全体を「失敗」と判定する。`;

function parseDataUrl(input: string): { mime: string; data: string } {
  const m = /^data:([^;]+);base64,(.+)$/i.exec(input.trim());
  if (m) return { mime: m[1], data: m[2] };
  return { mime: 'image/jpeg', data: input.trim() };
}

export const POST: APIRoute = async ({ request }) => {
  let body: ScanRequestBody;
  try {
    body = (await request.json()) as ScanRequestBody;
  } catch {
    return json({ error: 'Invalid JSON body' }, 400);
  }
  if (!body?.image) {
    return json({ error: 'image is required (data URL or base64)' }, 400);
  }

  const { mime, data } = parseDataUrl(body.image);
  const userParts: GeminiContent['parts'] = [
    { inline_data: { mime_type: mime, data } },
    {
      text: body.hint
        ? `補足: ${body.hint}\nこの紙面を Markdown に書き起こしてください。`
        : 'この紙面を Markdown に書き起こしてください。',
    },
  ];

  try {
    const res = await callGemini(
      import.meta.env.GEMINI_API_KEY,
      {
        systemInstruction: { parts: [{ text: ANALYZE_SYSTEM }] },
        contents: [{ role: 'user', parts: userParts }],
        generationConfig: {
          temperature: 0.0,
          maxOutputTokens: 32768,
          thinkingConfig: { thinkingBudget: 2048 },
        },
      },
      MODELS.scan,
    );
    const markdown = extractText(res);
    const finishReason = res.candidates?.[0]?.finishReason;
    return json({ markdown, finishReason });
  } catch (err) {
    if (err instanceof GeminiError) {
      return json(
        { error: err.message, detail: err.body },
        err.status >= 400 ? err.status : 500,
      );
    }
    return json({ error: 'Unexpected error', detail: String(err) }, 500);
  }
};

function json(data: unknown, status = 200): Response {
  return new Response(JSON.stringify(data), {
    status,
    headers: { 'content-type': 'application/json; charset=utf-8' },
  });
}
