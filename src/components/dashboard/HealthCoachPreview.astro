---
import AppIcon from '../../components/AppIcon.astro';
interface Props {
  greeting: string;
  /** Elith summary 1 行目 */
  summaryHint?: string | null;
  /** ?u= 伝搬用 */
  diagnosticUserId?: string;
}
const { greeting, summaryHint, diagnosticUserId } = Astro.props as Props;
const opener = summaryHint
  ? `${greeting}、${summaryHint.slice(0, 60)}...`
  : `${greeting}、検査結果や生活習慣について何でも聞いてください。`;
const href = diagnosticUserId ? `/coach?u=${encodeURIComponent(diagnosticUserId)}` : '/coach';
---

<section class="rounded-2xl bg-mist p-4 shadow-sm">
  <div class="flex items-start gap-3">
    <div class="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-brand-600 text-white">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <rect x="3" y="11" width="18" height="10" rx="2"/>
        <circle cx="12" cy="5" r="2"/>
        <path d="M12 7v4"/>
        <line x1="8" y1="16" x2="8" y2="16"/>
        <line x1="16" y1="16" x2="16" y2="16"/>
      </svg>
    </div>
    <div class="flex-1">
      <div class="flex items-center gap-2">
        <h3 class="text-sm font-bold text-slate-800">AI ヘルスコーチ</h3>
        <span class="rounded-full bg-emerald-500/20 px-2 py-0.5 text-[10px] font-medium text-emerald-700">
          ● オンライン
        </span>
      </div>
      <p class="mt-1 text-[12px] leading-snug text-slate-600">
        {opener}
      </p>
    </div>
  </div>
  <a
    href={href}
    class="mt-3 flex w-full items-center justify-between rounded-xl bg-white px-3 py-2.5 text-sm text-slate-500 shadow-sm transition-colors"
  >
    <span><AppIcon name="interview" size={20} /> コーチに相談する...</span>
    <span class="text-brand-600">→</span>
  </a>
</section>
