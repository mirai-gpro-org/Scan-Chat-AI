# AI疾病予防報告書 — 引き継ぎ書

| | |
|---|---|
| 対象 | パイプライン⑥「AI疾病予防報告書」の生成機能 |
| 状態 | **未実装。** 2026-08-29 の実装 (P0〜P4) は**リバート済み** (§4)。作り直しは仕様書から始める |
| デプロイ元 | Scan-Chat-AI `claude/awesome-carson-UeyUZ` / wellfort-site `claude/wellfort-ui-design-draft-7y8dup` |

**本書に書くのは ミッション・設計ポリシー・参照ドキュメント だけ。**
既存実装の説明は載せない (全てリバートするため)。

---

## 1. ミッション

### 1.1 第 1 層 — 事業のミッション

`docs/funding_application/要件定義書.md:27`

> がん及び生活習慣病を、健康モニタリングにより**早期発見および予防**し、
> 健康寿命を大幅に伸ばし、豊かな人生を実現する。

事業の核心 (同 `:31`) = 「**読ませるレポート**」から「**行動を促すダッシュボード**」へ。
**既存の検査結果報告書はテキスト中心で読解負荷が高く、行動に繋がらない — これが解くべき課題。**

### 1.2 第 2 層 — サービスの 2 本柱 (設計ポリシー)

`docs/elith/ai_prevention_report_generation_spec.md` §1.0 (発注者確認 2026-08-28)

```
年 1 回 人間ドック ＋ 年 3 回 血液検査・がんリスク検査   ← 隙間を埋める
              = 年 4 回の検査
                     │
      ┌──────────────┴──────────────┐
 A 検査結果そのもの            B その検査データで AI 診断を回す
 → 初期がんの早期発見           → 疾病予防アドバイス
```

- **B の入力は A の検査結果。別系統ではない。**
- **報告書の冒頭に、2 本柱をそのままトピックとして置く。**
- **章の並び・見出しも、この 2 本柱に沿えて構成する。**
- **枠 (2 本柱) は常設、中身 (章) だけが条件付き。**
  下位の運用ルール「材料が無い章は出さない」(spec §1.0.1) で枠を消してはならない。
- **ポリシーの説明文そのものは紙面に載せない。** ポリシーが求めるのは
  2 本柱が構造として最初に見えることであって、ポリシー文の掲載ではない。

### 1.3 第 3 層 — 本機能の目的

同 §1.1

> **Elith の出力は文章の羅列で、一般ユーザーが読み通せない。**
> 読みやすく、重要な点が確実に伝わる報告書を、受領データから**アプリが生成する**。

**役割は可読化だけ。** フォーマット変換でも内容の追加でもない。

### 1.4 優先順位

```
事業ミッション (読解負荷を下げ行動に繋げる)
   ↑ 従属
2 本柱 (画面設計はこれに従う)
   ↑ 従属
可読化のルール群 (章立て・材料が無い章は出さない・組版 …)
```

**下位の運用ルールを上位の目的より優先しないこと。**

### 1.5 越えてはならない線

| # | 制約 | 出典 |
|---|---|---|
| 1 | **Elith の出力以上の表記・表現を一切しない** | spec §1.0 (最重要・発注者指示) |
| 2 | アプリが医学的判断を生成しない (ミッション④: 整理して伝える／独自に分析・解釈しない) | spec §1.2 / `CLAUDE.md` |
| 3 | 受領データに無いものは作らない (捏造ゼロ) | spec §6 |
| 4 | 時系列・前回比をアプリで作らない (Elith が織り込み済み・二重の解釈になる) | spec §1.0.2 |
| 5 | 見本 PDF の**内容**を再現しない (様式のお手本であって項目一覧ではない) | spec §1.1 / §1.2 |
| 6 | 要望は (a) 見せ方=設定 / (b) 構造=コード / **(c) 内容の創作=不可** に仕分ける | spec §1.3.1 |

**1 と「可読化」は矛盾しない。** Elith の原文から**逐語で抜き出して構造に置く**のは可。
言い換え・要約・アプリ側の散文の追加は不可。

---

## 2. 参照すべきドキュメント

### 2.1 必読 (この順・**通読すること**)

| # | ドキュメント | 何が書いてあるか |
|---|---|---|
| 1 | `CLAUDE.md` | 作業ルールと確定事項の索引。冒頭「調査・推測のアンチパターン R1〜R5」 |
| 2 | `docs/elith/ai_prevention_report_generation_spec.md` | **本機能の正本 (1,435 行)** |
| 3 | `docs/funding_application/要件定義書.md` §1 | 事業ミッション・課題・解決方針 |

**spec の中で特に見落としやすい節**

| 節 | 内容 |
|---|---|
| §0.3 | 未確定のまま進める方法 — 「文言が未確定 → `app_config` のキーを空で用意。空なら非表示。決まったら admin から入れる」「Elith の新フィールドが未受領 → カードごと非表示。**アプリが代わりを書くことはしない**」 |
| §1.3 | 設計方針 9 項目。**変更され続ける前提で作る**。§1.3.1 の要望 3 分類 |
| §4.-1 | **2 本柱の帯は常設。章の出し分けより上位** |
| §4.0.1 | A の総合所見。`ui.cancer_screening_not_included` というキー名まで書いてある |
| §4.2 | 見本の仕掛けの採否表 (食事・運動・睡眠を表と箇条書きへ分解 = **採る** 等) |
| §4.2.1 | **「要注意」を使わない**。赤は救急サインだけ |
| §4.2.2 | 生活習慣は【現状評価】→【行動提案】のペア。維持/改善の自動分類はしない |
| §4.3 | 長文の扱い。**そのまま流すと現状と変わらない** |
| §6 | 作れないもの (捏造ゼロの境界) |
| §7 | データ品質ガード。§7.0 = 検証検体は**合成データ** |
| §9 | 実装計画 (P0〜P5) |

### 2.2 関連 (必要になったら)

| ドキュメント | 何のとき |
|---|---|
| `docs/lab/lab_data_pipeline_master_spec.md` | ⑥ が E2E のどこか。上り(①〜⑤)と下り(⑥)は別 S3 経路 |
| `docs/elith/elith_s3_data_handoff_spec.md` | Elith へ渡す側 (上り) の仕様 |
| `docs/architecture/data_integration_requirements.md` | PII 分離。**本人への画面表示には適用されない** (spec §4.0.0.1) |
| `docs/scan/health_age_simple_v7.0_spec.md` | ウェルネス年齢の改称・段階フォールバック |

### 2.3 素材

| | 場所 |
|---|---|
| 受領 JSON (2026-08-26・合成検体・PII なし) | `src/data/elith/report_text_20260826.json` / `health_checkup_20260826.json` |
| Stage2 PDF (2026-08-06・**がんリスク検査あり**の別人検体) | `docs/elith/2026_08_06_Elith_健康アドバイスレポート_Stage2.pdf` |
| 紙面モック タイプ2 (単品購入) | Artifact `6eac069b-6d12-4005-8b72-b2b3c020fadd` |
| 紙面モック タイプ1 (コースプラン) | Artifact `d633fc3f-4806-4ca4-86cf-45b13886b237` |
| Wellfort への確認依頼文 | Artifact `8561477c-4e46-4d37-801d-78b7494ce227` |

---

## 3. 回答待ち・仕様側の未確定

### 3.1 Wellfort / Elith の回答待ち (依頼文 Artifact `8561477c`)

| # | 内容 | 宛先 |
|---|---|---|
| 1 | **がんの観点での所見を毎回・必ず記載** ＋ 独立フィールド化 | Elith |
| 2 | 「最優先」の所見が 3 か所で食い違う (血圧 / ALT / 腹部超音波) | Elith |
| 3 | **がんリスク検査を含む検体の JSON 2 点** (タイプ1 の実装に必要) | Elith |
| 4 | `health_checkup.json` に基準値を付けてほしい | Elith |
| 5 | 出力形式の変更は事前連絡を (判定区分・`[pN]` が消えた実績) | Elith |
| 6 | 誤字「上上回っており」 | Elith |
| 7 | **がん早期発見欄の予備の文言の確定** → `ui.cancer_screening_not_included` へ入れる | Wellfort |
| 8 | アプリが受診の案内文を出してよいか | Wellfort |

### 3.2 仕様書の既知の不整合 (要修正)

**spec §6 ③ の末尾が古い。**

> それまでは A の章を**アプリのスキャン結果**で構成する (§4.0)

これは §4.0 (`:640`) で **撤回済み**。§1.0 とも矛盾する。

**「ミッション④」の原典が repo 内に無い。** `CLAUDE.md` と本 spec にしか現れない。

### 3.3 運用側

- ~~wellfort-site の GitHub 既定ブランチが `main` のまま~~ → **解決済み (2026-08-29・発注者対応)**。
  既定ブランチは `claude/wellfort-ui-design-draft-7y8dup` へ切替済み。
  **ただし切替前に `main` から切られたブランチは本番の 26 件を欠いたまま残っている。**
  既存ブランチで再開するときは、まず本番ブランチとの差分を確認すること
  (本リバート作業で実際に踏んだ)。

---

## 4. リバート (2026-08-29 実施済み)

2026-08-29 に入れた本機能の実装 (Scan-Chat-AI 11 件・wellfort-site 2 件) は
**全て誤った実装であり、リバート済み**。**確定仕様として参照しないこと。**

内容は `docs/elith/ai_prevention_report_REVERT_LIST.md`。要点だけ:

- **戻った**: `report.astro` は旧 3 モード (a/b/c) に、
  `api/admin/elith-report/upload.ts` は **PDF 必須**に戻っている。
- **消えた**: `report-adapter.ts` / `report-model.ts` / `report-sections.ts` /
  `api/admin/elith-report/audit.ts` / `scripts/verify-report-model.ts` /
  `app_config` の 5 キー (`ui.cancer_screening_not_included` ＋ `report.sections.*` 4 本)。
- **残した**: §2.3 の受領 JSON 2 点 (`src/data/elith/`)。参照コードは無いが素材として要る。

### 4.1 作り直す人が先に知っておくべき 2 点

1. **`elith-report-highlights.ts` は実データで無言で空になる。**
   `（判定区分：）` と `[pN]` に依存しているが、受領 JSON (2026-08-26) はどちらも **0 件**。
   旧サンプルの `[pN]` 10 件は**アプリが PDF 抽出時に付けたページマーカー**で、
   Elith 由来ではない (Stage2 PDF 原本も 0 件)。**サンプルでは動き実データで何も出ない。**
   → 検出は Elith 自身の判定文 (`基準範囲を上…` = 受領 JSON に 5 件) へ差し替える (spec §5.2)。
   `report.astro:7` が import しているので、片方だけ触るとビルドが通らない。
2. **`diagnosis.diagnosis_results.checkup_values` 列は DB に存在するが、使うコードが無い。**
   マイグレーション `20260829000010_diagnosis_report_checkup.sql` は **Supabase へ適用済み**
   (発注者確認 2026-08-29) なのでファイルも残してある。リバートで読み書きが消えたため
   **列は常に null**。受け皿は作り直しでも要る (spec §8.2) ので `drop column` はしない。
   コメントの食い違い (`report` 列が `elith-v2.0` に言及していた等) は
   `20260829000020_diagnosis_report_checkup_comment.sql` で直してある。**`db push` が要る。**
   **適用済みのマイグレーションは編集して当て直さないこと** — `db push` はスキップするので
   新環境にしか届かず、同じファイル名で中身の違う DB が並ぶ。

### 4.2 実装で得た実測 (設計判断ではなく事実)

`docs/elith/ai_prevention_report_generation_spec.md` §9.2 に表で残してある。
同じ穴を踏まないために、着手前に目を通すこと。
