# リバート一覧 — 2026-08-30 セッション (Claude)

本番ブランチ `claude/awesome-carson-UeyUZ` に入った 15 コミット。
**古い順**に並べる。リバートは**新しい順**（下から上へ）に行うと衝突が少ない。

すべて `git revert --no-commit <hash>` で個別に外せる。
マージコミットではなく**中身のコミット**を指定すること。

---

## 一覧

| # | hash | PR | 内容 | 触ったファイル | 外してよいか |
|---|---|---|---|---|---|
| 1 | `e672ca4` | #187 | 最小 SW（オフライン案内）とダッシュボードのタイル修正 | `public/sw.js` `public/offline.html` `src/layouts/BaseLayout.astro` `src/components/dashboard/ReportLinkCard.astro` `src/lib/report-model.ts` `src/pages/report.astro` | **発注者判断で入れたもの**（spec §4.4）。報告書の件とは無関係 |
| 2 | `6b9bc7f` | #188 | 紙面（白いシート）を作る＋`verify:screen` に「紙が在るか」チェック追加 | `src/styles/global.css` `src/pages/report.astro` `scripts/verify-screen.mjs` | **発注者指摘「モックと全く違う」への対応**。外すと紙面が無くなる |
| 3 | `65bb406` | #189 | 受領 JSON を本番へ実データとして入れる取込スクリプト | `scripts/ingest-elith-report.mjs`（新規） | **未使用**。外して支障なし |
| 4 | `a8ddd77` | #189 | `demoFallbackEnabled` の順序を ①admin→②env→③許可uid に変更＋`verify:demo-gate` 新規 | `src/lib/demo-data.ts` `scripts/verify-demo-gate.mjs` `package.json` | **発注者指示そのもの**（admin だけダミー）。外すと本番 env で admin が塞がれる |
| 5 | `815449a` | #189 | `ADMIN_MEMBERS` に管理者登録を統合（`ADMIN_EMAILS`/`ADMIN_UIDS` は導出へ） | `src/lib/admin-auth.ts` `scripts/verify-demo-gate.mjs` | 統合前は手書き 2 本がズレていた。外すと元のズレに戻る |
| 6 | `484d198` | #190 | 総合テストチーム向け変更連絡（ドキュメントのみ） | `docs/operations/総合テスト向け_入場制御とデモ表示_変更連絡.md`（新規） | ドキュメントのみ。外して支障なし |
| 7 | `a7835d7` | #190 | **admin 判定を email からも成立させる**＋閲覧者フラグを画面から判定関数まで引き回す | `src/lib/viewer.ts` `src/lib/demo-data.ts` `src/lib/dashboard-queries.ts` `src/lib/elith-report-queries.ts` `src/lib/measurement-queries.ts` `src/lib/notice-queries.ts` `src/pages/api/auth/resolve.ts` `src/pages/{dashboard,kit,notices,report,trend}.astro` ほか 16 ファイル | **影響範囲が最大**。Cookie の形式を 3 分割→4 分割に変更（旧形式も受ける）。外すなら #9 と一緒に |
| 8 | `975fe40` | #190 | 上記をテスト連絡に追記（ドキュメントのみ） | `docs/operations/総合テスト向け_…_変更連絡.md` | ドキュメントのみ |
| 9 | `b94bd7a` | #191 | admin の正を `public.admin_users` テーブル照会へ＋Cookie の自己修復 | `src/lib/admin-auth.ts` `src/lib/viewer.ts` `src/components/GoogleOneTap.astro` `src/pages/api/auth/resolve.ts` `src/pages/dashboard.astro` | #7 とセット。**外すと発行済み Cookie の admin フラグが更新されなくなる** |
| 10 | `d41701e` | #192 | **自己診断の口**を新設＋admin フラグを毎タブ取り直す | `src/pages/api/debug/viewer.ts`（新規） `src/pages/api/auth/refresh-admin.ts`（新規） `src/components/GoogleOneTap.astro` | **計測用**。原因特定後は外してよい |
| 11 | `4af7edf` | #193 | 診断口に受領データの中身の量（`latest_sections`/`latest_chars`）を追加＋記録 | `src/pages/api/debug/viewer.ts` `CLAUDE.md` `docs/elith/AI疾病予防報告書_仕様書.md` | 計測用 |
| 12 | `23973e2` | #194 | **旧形式（`elith-v1.0` 配列）の行を admin のデモ表示でだけ飛ばす** | `src/lib/elith-report-queries.ts` `scripts/verify-demo-gate.mjs` | **紙面の中身を変える分岐**。外すと admin は seed の旧行（2 セクション）を見る |
| 13 | `45507db` | #195 | 診断口が表示モデルの枚数を返すようにする | `src/pages/api/debug/viewer.ts` | 計測用 |
| 14 | `237abd0` | #196 | **`cache-control: private, no-store`** を 8 ページへ＋診断口が `/report` を自己 fetch | `src/lib/http-cache.ts`（新規） `src/pages/{chat,coach,dashboard,kit,notices,report,scan,trend}.astro` `src/pages/api/debug/viewer.ts` | **キャッシュ指示は残すことを推奨**（`public` は共有キャッシュに保存を許すため、Cookie 個人化ページでは他人の紙面が配られ得る）。自己 fetch は本番で `fetch failed` = **無効** |
| 15 | `d42b8ad` | #197 | `/report?diag=1` で画面が自分の値を書き出す | `src/pages/report.astro` `src/pages/api/debug/viewer.ts` | 計測用 |

---

## 用途別の外し方

### A. 計測用だけを外す（本番の挙動は変わらない）

```bash
git revert --no-commit d42b8ad 45507db 4af7edf d41701e
# ＋ 237abd0 の自己 fetch 部分（cache-control は残す）を手で外す
git commit -m "計測用の口を撤去"
```
消えるもの: `/api/debug/viewer` / `/api/auth/refresh-admin` / `/report?diag=1`

### B. 今回セッションを全部戻す

ブランチ履歴が一直線ではない（他ブランチのマージが挟まる）ため、
**範囲指定ではなく 15 個のハッシュを明示**して戻す。

```bash
git revert --no-commit d42b8ad 237abd0 45507db 23973e2 4af7edf \
                        d41701e b94bd7a 975fe40 a7835d7 484d198 \
                        815449a a8ddd77 65bb406 6b9bc7f e672ca4
git commit -m "2026-08-30 セッションを全て取り消す"
```

**注意**: 全部戻すと、紙面（白いシート）・admin だけダミーの入場制御・
最小 SW も**すべて消える**。これらは発注者指示で入れたもの。

### C. 推奨（判断を分ける）

| 残す | 外す |
|---|---|
| #1 SW・#2 紙面・#4 デモゲート順序・#5 ADMIN_MEMBERS 統合・#14 の `cache-control` | #10 #11 #13 #15（計測用）・#14 の自己 fetch・#3（未使用） |
| #7 #9（admin 判定）は**引き継ぎ先が原因を特定してから**判断 | #12 は紙面の中身を変えるので**引き継ぎ先の判断待ち** |

---

## 併せて外すもの（コードでない副作用）

| 対象 | 内容 |
|---|---|
| `CLAUDE.md` | 上記各コミットで**確定事項として追記した記述**が残る。リバートすると git 上は消えるが、**内容の正否は引き継ぎ先が再判断すること** |
| `docs/elith/AI疾病予防報告書_仕様書.md` | §4.3.5 / §4.4 / §4.5 / §4.5.1 / §4.6 / §7 の失敗 #6〜#8 |
| `docs/operations/総合テスト向け_入場制御とデモ表示_変更連絡.md` | 新規ファイル（#6 #8 #9） |
| `package.json` | `verify:demo-gate` / `verify:report` の 2 スクリプト（#4） |
| **DB** | このセッションでは**マイグレーションを追加していない**。`supabase db push` 待ちの `20260829000020` は**前セッションのもの**で、今回のリバート対象外 |
| **Vercel 環境変数** | 変更していない |

---

## 事実確認用

```bash
# 15 個の中身を 1 個ずつ見る
for c in e672ca4 6b9bc7f 65bb406 a8ddd77 815449a 484d198 a7835d7 975fe40 \
         b94bd7a d41701e 4af7edf 23973e2 45507db 237abd0 d42b8ad; do
  git show --stat --format='%h %s' $c
done

git show <hash>                # 個別の中身
```

**範囲指定 `5cd119b..b74dd37` は 14 個しか出ない**
（`e672ca4` はマージ `5cd119b`（PR #187）の内側にあるため）。
`6067b18..b74dd37` は 17 個出るが、**他ブランチ由来の 2 件が混ざる**。
確実なのは上の 15 ハッシュの明示。
