# スキャン回帰用ゴールデン（人間ドック: 受診日 2024-09-24 / merged / rows=100）

第2検体。検診(2025-01-23・`scan_golden_healthcheckup_20250123.md`)とは別系統の**人間ドック様式**
(多テーブル・「検査結果の推移」グラフ・オプション検査・診察/所見)で、課題A/B/C の回帰を測る。
プロンプト/後段変更ごとに再RUN→照合器(`elith-batch.astro goldenCheck`)で比較する。

- 検体: `scan-accuracy-test/user/test-202608022302-001/date/2024_09_24/HealthCheckupData_date_2024_09_24_user_test-202608022302-001.json`
- model: `gemini-3.1-flash-lite` / kind: `scan_merged`
- **前提**: 今回(最新回)列のみ納品対象。前回列は Shift/Leak 検知の正解に併記する。

## この検体で確認された課題（🔍 LLM照合 2026-08-02・env 新機能OFF の baseline）

**基礎精度は高い**: 一致率 98%（画像実施105 / JSON100）・**誤読0・捏造0**。残る不具合は課題A/C:

### 課題A: 行ごと取りこぼし（2件・今回値あるのにJSONに無い）
| 項目 | ページ | bbox(画像) | 判定/所見 | 納品対象か | 今回値(正解) |
|---|---|---|---|---|---|
| 内科診察 | p3 左 | 435,335,475,417 | 判定A | **要決定**(判定/所見の扱い) | 【要転記】 |
| 上部消化管 | p5 オプション | 152,245,222,332 | 判定E・所見あり | **要決定** | 【要転記】 |

> **要決定(実画像で判断)**: 内科診察=判定A・上部消化管=判定E+所見 は numeric でなく**判定/所見**。
> 現行 `sanitizeMeasurementsForDelivery` は総合判定(A/B/C)欄を除外する。Elith 納品に含めるべきか、
> 含めるならどの値(判定記号/所見テキスト)を正解とするかを、実画像を見て確定してから golden に入れる。
> (LLM grader は"取りこぼし"と言うが、判定/所見は正当に除外される場合もある＝捏造ゼロの範囲で判断)

### 課題C: 別名/ページ跨ぎ重複（2件・マージ由来）
| 項目 | 重複位置 | 今回値(正解) | 備考 |
|---|---|---|---|
| 体重 | JSON idx 1 と idx 9 | 【要転記】 | 同値なら dedup が統合(SCAN_OBS_DEDUP)。別値なら競合として要確認 |
| 眼底・前眼部 | JSON idx 22 と idx 23 | 【要転記】 | 同上。標準マスタ未収録名→ semanticKey=正規化名で統合 |

### 課題B: 推移グラフ混入
- この run の baseline では **誤読0**（＝この検体では顕在化せず）。推移グラフ section の有無・
  グラフ由来 False-Value の回帰番人は、raw_markdown 確認後に該当項目を追加する。

### VQA第2パス(SCAN_BOUNDARY_RECHECK=on)の実績（この run）
- 充填2: 蛋白=(-) / 潜血=(-)（尿定性を正しく充填）。
- 未解決5: K-W分類右/左=空・蛋白(1件目)=空・免疫便潜血 1日目=A?/2日目=空（run毎の非決定・要確認）。
  → 免疫便潜血 1日目 VQA=A は要確認（(-)/(+) 以外＝許可集合外の疑い。実画像で正解を確定）。

## 全今回値（実施項目・要転記）

> **【要転記】この run は誤読0・捏造0のため、納品JSON(rows=100)の今回値がほぼ正解。
> raw_markdown(📄DL) or JSON を受領し、①重複2を1件に整理 ②取りこぼし2の正解値を実画像で確定
> ③各項目 `{name, today, past?, alt?, q?}` へ転記 して `GOLDEN_HC_20240924` を確定する。
> 捏造ゼロのため、値が確認できるまでこの節は空とする(推測で埋めない)。**

（受領後にここへ実値を転記）

## 照合器への追加（要実装・wellfort-site `elith-batch.astro`）
1. `GOLDEN_HC_20240924` 配列 + セクション別カバレッジ `GOLDEN_SEC_20240924`（人間ドックのページ構成）。
2. `goldenCheck` を **検体切替**（読み込んだ JSON の `test_date`/`kind` で 2025-01-23検診 / 2024-09-24人間ドック を選択）。
3. **課題A/B/C指標の追加**: 概念重複数(dedup前後)・行欠落(期待項目 vs JSON)・グラフ由来 False-Value。
   （現行 goldenCheck は missing/shift/wrong/falseVal/tie + セクションカバレッジ。重複検知を追加する。）
